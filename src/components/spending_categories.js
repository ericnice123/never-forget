import React, {Component} from 'react';
import {View, Text} from 'react-native';

class SpendingCategories extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        console.log(this.props.numOfItems);
        return(
            <View>
                
            </View>
        );
    }
}

export default SpendingCategories;